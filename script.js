const obs=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));
const menu=document.querySelector('.menu');document.querySelector('.hamb').addEventListener('click',()=>menu.classList.toggle('open'));document.querySelectorAll('.menu a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));
const chips=[...document.querySelectorAll('.chip')],cards=[...document.querySelectorAll('.card')];chips.forEach(ch=>ch.addEventListener('click',()=>{chips.forEach(x=>x.classList.remove('active'));ch.classList.add('active');cards.forEach(c=>c.style.display=(ch.dataset.filter==='all'||c.dataset.cat===ch.dataset.filter)?'block':'none')}));
const lb=document.querySelector('.lightbox'),lbImg=lb.querySelector('img');cards.forEach(c=>c.addEventListener('click',()=>{lbImg.src=c.dataset.full;lb.classList.add('open')}));lb.addEventListener('click',e=>{if(e.target===lb||e.target.classList.contains('close'))lb.classList.remove('open')});document.addEventListener('keydown',e=>{if(e.key==='Escape')lb.classList.remove('open')});
const mainVideo=document.querySelector('#mainVideo'),mainSource=document.querySelector('#mainSource');document.querySelectorAll('.vthumb').forEach(v=>v.addEventListener('click',()=>{mainSource.src=v.dataset.video;mainVideo.poster=v.dataset.poster;mainVideo.load();mainVideo.play().catch(()=>{});document.querySelector('#films').scrollIntoView({behavior:'smooth'})}));
